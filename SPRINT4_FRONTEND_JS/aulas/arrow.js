const soma = function(a, b) {
    return a + b;
}
const soma = (a, b) => {
    return a + b;
}

const soma = (a, b) => a + b;

function imprimeTexto(texto) {
    return texto
}

const imprimeTexto = texto => texto

ola = () => "olá"
ola = _ => "olá"